<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/TimeRecord_detailImageView"
        android:layout_width="18dp"
        android:layout_height="18dp"
        app:layout_constraintBottom_toBottomOf="@+id/TimeRecord_photoButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/TimeRecord_photoButton"
        app:srcCompat="@drawable/ic_computer_black_36dp"
        app:tint="@color/Apricot" />

    <TextView
        android:id="@+id/TimeRecord_detailGoalTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:fontFamily="@font/noto_sans"
        android:text="세부 목표"
        android:textColor="@color/Black"
        android:textSize="14sp"
        app:layout_constraintBottom_toBottomOf="@+id/TimeRecord_photoButton"
        app:layout_constraintStart_toEndOf="@+id/TimeRecord_detailImageView"
        app:layout_constraintTop_toTopOf="@+id/TimeRecord_photoButton" />

    <ImageButton
        android:id="@+id/TimeRecord_photoButton"
        android:layout_width="70dp"
        android:layout_height="24dp"
        android:background="@drawable/record_time_button_box"
        android:backgroundTint="@color/Yellow"
        android:src="@drawable/ic_photo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>